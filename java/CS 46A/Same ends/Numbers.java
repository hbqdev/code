public class Numbers
/**
* Create a class that contains a method to compute the longest run of a sequence and returns its value
*/
{
/**
      Computes the length of the ends of sequence (if the two ends are the same).
      @param values an array of integer values
      @return the length of the same ends in values
   */
 public static int sameEnds(int[] values)
   {
   for (int i=values.length/2;i>0;i--)
   {
    
  // boolean thisMatch = true;
   //for (int j =0;j<i; j++)
   //{
      // if (values[j] != values[values.length-i+j])
       //thisMatch = false;
    //}
    //if (thisMatch)
    //return i;
}
return 0;
}        
private boolean checkOneLength(int [] values, int length)
{
for (int j=0; j< length; j++)
if (values[
}
}

